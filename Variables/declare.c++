#include <iostream>

int main(){
    //declaring the same type of data type,separate using a comma
    int x = 5, y = 6, z = 7;
    std::cout << x+y+z << std::endl;

    int a,b,c;
    a = b = c = 80;
    std::cout << a*b-c << std::endl;

    return 0;
}